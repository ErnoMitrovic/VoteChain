'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var util = require('@polkadot/util');
var CreateType = require('./CreateType.js');

function createPayload(payload, type, types) {
    if (payload === undefined) {
        return '0x';
    }
    if (util.isHex(payload)) {
        return payload;
    }
    if (util.isU8a(payload)) {
        return util.u8aToHex(payload);
    }
    return CreateType.CreateType.create(type, payload, types).toHex();
}

exports.createPayload = createPayload;
