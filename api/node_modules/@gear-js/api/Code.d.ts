/// <reference types="node" />
import { ISubmittableResult } from '@polkadot/types/types';
import { SubmittableExtrinsic } from '@polkadot/api/types';
import { CodeStorage, Hex } from './types';
import { GearTransaction } from './Transaction';
export declare class GearCode extends GearTransaction {
    /**
     * ### Submit code without initialization
     * @param code
     * @returns Code hash
     */
    upload(code: Buffer | Uint8Array): Promise<{
        codeHash: Hex;
        submitted: SubmittableExtrinsic<'promise', ISubmittableResult>;
    }>;
    /**
     * ### Check that codeId exists on chain
     * @param codeId
     */
    exists(codeId: string): Promise<boolean>;
    /**
     * ### Get code storage
     * @param codeId
     */
    storage(codeId: Hex): Promise<CodeStorage>;
    /**
     * ### Get static pages of code
     * @param codeId
     */
    staticPages(codeId: Hex): Promise<number | null>;
    /**
     * ### Get all ids of codes uploaded on connected chain
     * @returns array of code ids uploaded on chain
     */
    all(): Promise<Hex[]>;
}
