import { ActiveProgram, IGearPages, Hex } from './types';
import { GearApi } from './GearApi';
export declare class GearStorage {
    protected _api: GearApi;
    constructor(_api: GearApi);
    /**
     * Get program from chain
     * @param programId
     * @returns
     */
    gProg(programId: Hex): Promise<ActiveProgram>;
    /**
     * Get list of pages for program
     * @param programId
     * @param gProg
     * @returns
     */
    gPages(programId: Hex, gProg: ActiveProgram): Promise<IGearPages>;
}
