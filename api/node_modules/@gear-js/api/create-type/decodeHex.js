import { TypeInfoRegistry } from './TypeInfoReg.js';

function decodeHexTypes(hexTypes) {
    const typeInfoReg = new TypeInfoRegistry(hexTypes);
    return typeInfoReg.getTypes();
}

export { decodeHexTypes };
