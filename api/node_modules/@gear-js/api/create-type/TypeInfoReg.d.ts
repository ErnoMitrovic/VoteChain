import { Hex } from '../types';
import { TypeRegistry } from '@polkadot/types';
import { Codec } from '@polkadot/types/types';
export declare class TypeInfoRegistry {
    #private;
    registryTypes: Uint8Array;
    registry: TypeRegistry;
    constructor(registryTypes: Hex | Uint8Array);
    getShortName(fullName: string): string;
    getTypes(): Record<string, string>;
    createType(typeName: string, data: any): Codec;
    getGenericName(type: string): string;
}
