import { Codec } from '@polkadot/types/types';
import { Hex } from '../types';
export declare function createPayload(payload: unknown, type: string, types: Hex | Uint8Array): Hex | Uint8Array | Codec;
