/// <reference types="node" />
import { Hex } from '../types';
export declare function generateCodeHash(code: Buffer | Uint8Array | Hex): Hex;
export declare function generateProgramId(codeId: Hex | Uint8Array, salt: string | Hex | Uint8Array): Hex;
export declare function generateProgramId(code: Buffer | Uint8Array, salt: string | Hex | Uint8Array): Hex;
